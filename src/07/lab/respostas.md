# Atividade Avaliativa 2 - Respostas

Nome:

GRR:

Se a atividade foi feita em dupla, escreva aqui os dois nomes e os dois GRR. As
duas pessoas enviam o mesmo `.zip` na UFPR Virtual.

Ambiente (processador, sistema operacional, versão do Go):

## Parte 1

1. Em uma frase, como a sua `EhABB` rejeita a árvore que tem o 60 na subárvore
   esquerda do 50, sendo que o 60 é maior que o pai 30?

2. Em uma frase, o que a sua `Sucessor` guarda durante a descida, e por que isso
   basta quando `v` não está na árvore.

## Parte 2

3. Qual era o defeito da `RotacaoDireita`? Escreva a linha errada e a linha
   corrigida.

4. Rodando `./alturas` **antes** da correção, qual nó a sua `VerificaAVL`
   acusou, e qual foi a divergência (campo `Alt`, altura recalculada, fator)?

## Parte 3

Saída de `./alturas` antes da correção do defeito:

```
cole aqui o conteúdo de antes.txt
```

Saída de `./alturas` depois da correção:

```
cole aqui o conteúdo de depois.txt
```

5. Na Tabela 1, a ABB chega à altura `n-1` e a AVL fica em torno de
   `log2(n)`. Explique a diferença a partir do que acontece a cada inserção.

6. Antes da correção, a Tabela 1 saía certa e a Tabela 2 saía errada. Qual
   rotação cada uma das duas sequências exercita, e por que só uma delas
   expunha o defeito?

## Uso de IA

Diga se usou ferramentas de IA generativa nesta atividade e em que partes. Não
influencia a nota.
